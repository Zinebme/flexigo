import type {ReactNode} from "react";import type {StorefrontData} from "@/lib/storefront/data";import {loadSouqShop} from "@/lib/storefront/souq/catalog";import {pulseCssVars} from "@/lib/storefront/pulse/tokens";import {PulseContainer} from "./pulse-ui";import "./pulse.css";
export async function PulseShell({data,children}:{data:StorefrontData;children:ReactNode}){const shop=await loadSouqShop(data.id);const base=data.base;const contact=data.settings?.contact;const nav=[{href:base,label:"الرئيسية"},{href:`${base}/boutique`,label:"المتجر"},{href:`${base}/boutique?sort=featured`,label:"الأكثر طلباً"},{href:`${base}/boutique?filter=offers`,label:"العروض"},{href:`${base}/faq`,label:"الأسئلة"}];return <div dir="rtl" className="pulse-root min-h-screen" style={pulseCssVars()} data-template="pulse-v1"><div className="bg-[var(--pulse-dark)] px-3 py-2 text-center text-[11px] font-bold text-white">{data.theme?.announcement?.trim()||"توصيل 58 ولاية • الدفع عند الاستلام"}</div><header className="sticky top-0 z-50 border-b border-[var(--pulse-border)] bg-white/95 backdrop-blur"><PulseContainer className="flex h-16 items-center justify-between gap-3"><a href={base} className="flex items-center gap-2 font-black">{data.theme?.logo_url?<img src={data.theme.logo_url} alt={data.name} className="h-9 max-w-[150px] object-contain"/>:<><span className="flex h-9 w-9 items-center justify-center rounded-xl bg-[var(--pulse-lime)] text-[var(--pulse-dark)]">P</span><span>{data.name}</span></>}</a><nav className="hidden gap-5 lg:flex">{nav.map(n=><a key={n.href} href={n.href} className="text-xs font-bold text-slate-600 hover:text-[var(--pulse-green-dark)]">{n.label}</a>)}</nav><div className="flex items-center gap-2">{contact?.whatsapp?<a href={`https://wa.me/${contact.whatsapp.replace(/\D/g,"")}`} className="rounded-xl border px-3 py-2 text-xs font-bold">واتساب</a>:null}<a href={`${base}/boutique`} className="pulse-btn-lime min-h-10 px-4 text-xs">تسوّق</a></div></PulseContainer><div className="border-t border-[var(--pulse-border)] lg:hidden"><PulseContainer className="flex gap-4 overflow-x-auto py-2 text-xs font-bold text-slate-500">{nav.slice(0,4).map(n=><a className="shrink-0" key={n.href} href={n.href}>{n.label}</a>)}</PulseContainer></div></header><main>{children}</main><footer className="mt-12 bg-[var(--pulse-dark)] py-10 text-white"><PulseContainer className="grid gap-8 sm:grid-cols-3"><div><div className="text-lg font-black">{data.name}</div><p className="mt-2 text-xs leading-6 text-white/60">اختيارات رياضية عملية للحركة والتمرين.</p></div><div><div className="text-xs font-black text-[var(--pulse-lime)]">الأقسام</div><div className="mt-3 flex flex-col gap-2 text-xs text-white/65">{shop.categories.slice(0,5).map(c=><a key={c.id} href={`${base}/categorie/${c.slug}`}>{c.name}</a>)}</div></div><div><div className="text-xs font-black text-[var(--pulse-lime)]">تواصل</div><div className="mt-3 space-y-2 text-xs text-white/65">{contact?.phone?<a className="block" href={`tel:${contact.phone}`}>{contact.phone}</a>:null}{contact?.email?<a className="block" href={`mailto:${contact.email}`}>{contact.email}</a>:null}</div></div></PulseContainer></footer></div>}
