import type {ReactNode} from "react";import {formatSouqPrice} from "@/lib/storefront/souq/format";import type {StoreLanguage} from "@/lib/types";
export function PulseContainer({children,className=""}:{children:ReactNode;className?:string}){return <div className={`pulse-container ${className}`}>{children}</div>}
export function PulseHeading({title,subtitle,eyebrow,action}:{title:string;subtitle?:string|null;eyebrow?:string|null;action?:ReactNode}){return <header className="pulse-section-head"><div>{eyebrow?<div className="pulse-kicker">{eyebrow}</div>:null}<h2 className="pulse-title mt-2 text-2xl sm:text-4xl">{title}</h2>{subtitle?<p className="mt-2 max-w-2xl text-sm leading-7 text-[var(--pulse-muted)]">{subtitle}</p>:null}</div>{action}</header>}
export function PulsePrice({cents,compare,lang,currency="DZD"}:{cents:number;compare?:number|null;lang:StoreLanguage|string|null;currency?:string}){return <div className="flex items-baseline gap-2"><strong className="text-lg text-[var(--pulse-green-dark)]">{formatSouqPrice(cents,lang,currency)}</strong>{compare&&compare>cents?<span className="text-xs text-slate-400 line-through">{formatSouqPrice(compare,lang,currency)}</span>:null}</div>}
