import Link from "next/link";import type {StorefrontData} from "@/lib/storefront/data";import {loadSouqShop} from "@/lib/storefront/souq/catalog";import {PulseContainer,PulseHeading} from "./pulse-ui";import {PulseProductCard} from "./pulse-product-card";
export async function PulseShopPage({data,filter}:{data:StorefrontData;filter?:string|null;sort?:string|null}){const {products,categories}=await loadSouqShop(data.id);const list=filter==="offers"?products.filter(p=>p.compareAtPriceCents&&p.compareAtPriceCents>p.priceCents):products;return <PulseContainer className="py-8"><PulseHeading eyebrow="SPORT STORE" title={filter==="offers"?"العروض":"المتجر"} subtitle="كل ما تحتاجه للحركة والتمرين." />{categories.length?<div className="mb-6 flex gap-2 overflow-x-auto">{categories.map(c=><Link key={c.id} href={`${data.base}/categorie/${c.slug}`} className="shrink-0 rounded-full border bg-white px-4 py-2 text-xs font-bold">{c.name}</Link>)}</div>:null}<div className="grid grid-cols-2 gap-3 sm:grid-cols-3 lg:grid-cols-4 sm:gap-5">{list.map(p=><PulseProductCard key={p.id} product={p} base={data.base} lang={data.lang} currency={data.currency}/>)}</div></PulseContainer>}
export async function PulseCategoryPage({data,category}:{data:StorefrontData;category:{id:string;name:string;slug:string;description:string|null}}){const {products}=await loadSouqShop(data.id);const list=products.filter(p=>p.categoryId===category.id);return <PulseContainer className="py-8"><PulseHeading eyebrow="CATEGORY" title={category.name} subtitle={category.description}/><div className="grid grid-cols-2 gap-3 sm:grid-cols-3 lg:grid-cols-4 sm:gap-5">{list.map(p=><PulseProductCard key={p.id} product={p} base={data.base} lang={data.lang} currency={data.currency}/>)}</div></PulseContainer>}
