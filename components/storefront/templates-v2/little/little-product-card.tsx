import type {SouqProductSummary} from "@/lib/storefront/souq/catalog";import type {StoreLanguage} from "@/lib/types";import {StorefrontImage} from "../../image";import {LittlePrice} from "./little-ui";
export function LittleProductCard({product,base,lang,currency="DZD"}:{product:SouqProductSummary;base:string;lang:StoreLanguage|string|null;currency?:string}){const href=`${base}/produit/${product.slug}`;return <article className="little-product group flex h-full flex-col"><a href={href} className="relative block aspect-[4/3] overflow-hidden bg-[#FFF2F7]">{product.image?<StorefrontImage src={product.image} alt={product.name} fill sizes="(max-width:480px) 48vw,25vw" className="object-cover transition duration-500 group-hover:scale-105"/>:<div className="flex h-full items-center justify-center text-5xl">🧸</div>}<span className="little-chip absolute start-2 top-2">{product.isFeatured?"محبوب":"LITTLE"}</span><span className="absolute end-3 top-3 little-wiggle text-xl">✦</span></a><div className="flex flex-1 flex-col p-4">{product.categoryName?<div className="text-[10px] font-bold text-[var(--little-muted)]">{product.categoryName}</div>:null}<a href={href} className="mt-1 line-clamp-2 text-sm font-extrabold leading-6">{product.name}</a><div className="mt-auto pt-3"><LittlePrice cents={product.priceCents} compare={product.compareAtPriceCents} lang={lang} currency={currency}/></div><a href={href} className="mt-3 inline-flex min-h-10 items-center justify-center rounded-full bg-[var(--little-pink)] px-4 text-xs font-extrabold text-white">شوفي التفاصيل</a></div></article>}
